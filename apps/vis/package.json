{
  "name": "@odysseythink/vis",
  "version": "0.1.1",
  "private": true,
  "description": "Debug visualization tool for kimi-code sessions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:deps": "pnpm --filter @odysseythink/agent-core... build",
    "predev": "pnpm run build:deps",
    "dev": "node scripts/dev.mjs",
    "build": "pnpm run build:deps && pnpm --filter @odysseythink/vis-server build && pnpm --filter @odysseythink/vis-web build && node scripts/copy-web-dist.mjs",
    "prestart": "pnpm run build",
    "start": "node server/dist/server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
