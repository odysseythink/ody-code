---
"@odysseythink/agent-core": minor
"ody-code": minor
---

Add automatic and manual session checkpointing with integrity checks, versioned backups, and a recovery fallback chain. Users can trigger a `/checkpoint` save at any time, and checkpoints are written automatically at design/plan mode exits and turn boundaries.
