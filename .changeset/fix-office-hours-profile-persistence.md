---
"@odysseythink/agent-core": patch
"ody-code": patch
---

Auto-approve the `AppendBuilderProfile` tool and strengthen the office-hours prompt so builder profile entries are persisted reliably before the session exits.
