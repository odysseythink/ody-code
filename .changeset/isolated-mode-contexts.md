---
"@odysseythink/agent-core": minor
"ody-code": minor
---

Isolate normal/plan/design into separate persistent context threads and add a design→plan→normal handoff chain that carries the artifact file content between stages, replacing the previous single shared conversation across modes.
