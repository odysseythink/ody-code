---
"@odysseythink/agent-core": minor
"@odysseythink/kimi-code-sdk": minor
"@odysseythink/kimi-code": minor
---

Replace separate `planMode` and `designMode` fields with a unified `sessionMode` enum across the CLI, SDK, and TUI.
