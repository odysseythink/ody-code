---
"@odysseythink/agent-core": minor
"@odysseythink/ody-code-sdk": minor
"ody-code": minor
---

Add real-time progress spinner to the `/request-code-review` TUI command, streaming stage updates (preparing, fetching-diff, generating, etc.) from the executor through the RPC layer and SDK to the terminal.
